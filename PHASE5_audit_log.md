# Phase 5 · Audit Log — 收尾与叙事

> **状态**: ✅ 已收官 · 待打 release `v1.0`
> **在线**: https://chinese-regional-nev-marketing-sandbox-simulator-3lpxhv6sebdda.streamlit.app/
> **范围**: 不新增功能。`ensure_db.py`(修 bug) + `app.py`(象限地图 CSS Grid) + `copy_cn.py`(文案) + 叙事层(README / 演示路径 / About)
> **引擎四件套零改动**: `simulate.py` / `financials.py` / `game.py` / `calibration.py`
> **上游依赖**: Phase 0–4 全部收官并打 tag（`v0.1`~`v0.5`）
> **头号风险**: 为"再完善一点"而返工已冻结代码 → 一切改动过宪章 §8 三问

Phase 5 的目标不是"做得更多"，而是让这套东西**被人看懂、被人玩到、被人记住**。
本文件记录本阶段的改动、闭合的 open items，以及**被判定为"不做"的事**（后者同样是决策，需留档）。

---

## A. 已冻结的设计决策 (Frozen Decisions)

| # | 决策 | 依据 | 状态 |
|---|---|---|---|
| P5.1 | Phase 5 **不新增功能**；只做修 bug / 框架清晰度 / 趣味三类改动 | 宪章 §8 变更闸门 | ✅ 冻结 |
| P5.2 | `ensure_db()` 判据由"文件存在"改为"**存在且可用**"（非 0 字节 + 含所需表 + 是合法 sqlite） | 文件在 ≠ 库能用；空壳库会让参数恢复表整片 `no such table` | ✅ 冻结 |
| P5.3 | `REQUIRED_TABLES` 只列 `calibration.py` **实际查询**的五张表，不列齐 Phase 0 的六张 | 判据应为"够不够我读"，非"表齐不齐"；`regional_infra` 未被恢复链路读取 | ✅ 冻结 |
| P5.4 | 降级到 mock/重生成数据时置 `IS_FALLBACK`，界面**显式标注** | 项目立身之本是"β/γ 由回归恢复"；静默跑在 mock 上出绿勾比报错更危险 | ✅ 冻结 |
| P5.5 | 兜底路径**不修税 bug**：option B 是容错分支，非主动重跑 Phase 0；改 DGP 会让重生成库与 force-add 库不一致 | 宪章 §8：搭车修的前提是"因别的原因需重跑 Phase 0" | ✅ 冻结 |
| P5.6 | 首屏**不给未做出的决定下裁决**：四控件全初始位 → 只描述牌面 + 召唤动作 | 裁决句应只对用户做过的事发言；且这是起点态口径矛盾的病根 | ✅ 冻结 |
| P5.7 | 「牌面」（城市 / 象限）与「显示开关」（评估指标）**不计入初始态判据** | 宪章 §6 修订：动作预算只约束「我的动作」组；换城市 ≠ 做了决定 | ✅ 冻结 |

---

## B. 决策演变台账（被推翻/撤销的，务必留档）

| 议题 | 演变 | 最终 | 原因 |
|---|---|---|---|
| **option B（首boot 重生成）是否真的可用** | 四个阶段一直标"留作 v0.3 切换"，默认它只是没接入口名 → **实测发现是整条路径从未跑通** | 本阶段修好 | 见 §C1。走 `git add -f` 路线时该分支从未被执行，故 bug 一直未暴露 |

---

## C. 改动 (Changes)

### C1. `ensure_db.py` — 部署自举修复

三处改动，一个文件：

**① 可用性判据（`db_usable()`）**
`exists()` 会被三种坏文件骗过：0 字节占位、Git LFS 指针、空壳库（合法 sqlite 但 0 张表）。现改为三重判据：

```python
exists + st_size >= 1024 + sqlite_master 含 REQUIRED_TABLES
```

连接用 `mode=ro` URI，避免 `sqlite3.connect` 对不存在的路径顺手创建出空文件——那正是要检测的病。`sqlite3.DatabaseError` 一并接住（挡"根本不是 sqlite"）。

五种情形实测：文件不存在 / 0 字节 / 空壳(8KB, 0 表) / 非 sqlite 垃圾 → 全 `False`；健康库 → `True`。

**② option B 落盘（闭合 Phase 1 §G2）**
真实入口名为 **`generate_data.build()`**（`generate_data.py:33`）。但更关键的发现：**`build()` 只 `return tables, ground_truth`，不落盘**——全库 `grep to_sql|sqlite3|nev.db` 零命中。故原猜名循环即使猜中也必然失败。现由 `ensure_db.py` 自行持久化：六张 DataFrame `to_sql` 写入 `nev.db`，`ground_truth` 写 `ground_truth.json`。**option B 路径首次真正可用。**

> `build()` 返回 `model_dim / sales_transactions / macro_shocks_log / regional_infra / supply_chain_costs / financial_snapshots`，覆盖 §P5.3 所需五张表。

**③ 坏文件先删 + `IS_FALLBACK` 标志**
不可用的旧文件会阻塞后续写入 → 进入重生成前 `DB.unlink()`。
`IS_FALLBACK` 供 `app.py` 判断是否显示"演示数据"提示（文案走 `copy_cn.py`，不硬编码）。

---

### C2. 象限地图 — `st.columns` → CSS Grid（`app.py`）

**病症**（375px 竖屏 + 874px 横屏实测）
- 竖屏：三列整列竖堆，`[1, 0.06, 1]` 的轴列变成**一整行**，Q1 与 Q2 之间撑出半屏空白，2×2 语义消失。
- 横屏：列不塌，但四格**高度参差**。根因是原版靠 `min-height: 96/210` 对齐，而 `min-height` 只保证下限——内容顶破就各长各的。

**判断**：`st.columns` 是布局工具、不是网格，两条都治不了。象限地图承载「双维定位」的空间隐喻（`powertrain_focus × price_tier`），塌成一列这张图就不成立。故整体换实现，属**提升趣味/直觉**，过 §8 第三问。

**改法**（`_quad_card` / `render_quadrant_map` 重写，签名不变，四个调用点零改动）

| 项 | 原 | 现 |
|---|---|---|
| 布局 | `st.columns([1,0.06,1])` × 2 排 | 单个 CSS Grid，`grid-template-rows: 1fr auto 1fr` |
| 等高 | `min-height` 魔数 96/210 | grid 行高 `1fr`，与内容多少无关 |
| 纵轴 | 独立的 0.06 列 + `.vline` | `.qm-grid::after` 绝对定位覆在正中，**不占列** |
| 横轴 | 两列 + `.hline` | `.qm-xband` `grid-column: 1/-1` 横跨两列 |
| 卡片渲染 | `_quad_card(col, ...)` 写进 col | `_quad_card_html(q, ...)` 返回串，整网格一次 `st.markdown` |

**窄屏策略**：`@media (max-width:640px)` 只缩字号、只留 `feature` 一行（其余标 `.qm-more` 隐藏），**布局绝不换单列**——一列就没有象限地图了。被藏的三行由新增 expander 兜住。另修窄屏下横轴文字溢出裁切（`overflow-wrap` + 内边距）。

**踩过的坑（本地肉眼看不出，记此备查）**
> `.qm-card{ height:100%; margin:...; }` —— 盒高 = `100% + 上下 margin`，**必然溢出格子**，溢出部分正好压在横轴线上。表现为"横线与象限框重叠"，且**与是否高亮无关**（高亮时多 3px 发光环，只是更显眼）。
> 首次修法误判为"间距不够"，加大 padding 治标未治本。**正解：间距由 `.qm-cell` 的 padding 提供，卡片 `margin:0`。**

**连带清理**：旧 CSS 中 `.qgrid` / `.qcard` / `.vline` / `.hline` / `.axis-t` / `.axis-e` 已全部成为孤儿，一并删除。新 CSS 沿用原色板取值（线 `#C7D4CF`、正文 `#3E4F49`、弱化 `#8A9A93`），桌面端视觉基本无变化。

### C3. `copy_cn.py` — 两处文案（纯文案层，热重载，不重跑）

1. **新增 `QUAD_DETAILS_LABEL`**：象限完整说明的折叠标签，带「手机端建议点开」提示。按 P2.11，即使只是一根字符串也走文案层，不硬编码进 `app.py`（`app.py` 侧用 `getattr` 读并留兜底，老 `copy_cn` 不会崩）。
2. **改 `VERDICT_P3["CREATE"]` 措辞**：句尾「（独赢或共赢需看图判断。）」→「对手是否也在创造价值，看下方图二。」
   理由：原句把判断推给用户，撞宪章 §6「结论前置」。且 `game.py:318` 的原注释本就写着此态「读数再点明群体共创 / 你独善其身」——改措辞是**落实当年设计意图**，非新增语义。
   **未采纳**：由已有的对手 spread 数据直接判定独赢/共赢。那等于给 CREATE 拆出新态、改动 Phase 3 冻结的裁决定义，撞 §8 第一问。

> **附带澄清（防后人重走）**：`VERDICT_P3` 是**六态**不是四态——`CREATE_TRAIL` 在 `copy_cn.py:273` 以 `VERDICT_P3["CREATE_TRAIL"] = (...)` 追加式定义，离字典字面量较远，只读字面量会漏看。其判定逻辑早在 `game.py:346` 就已实现（`spread_rank > n_valid/2`）。**Phase 5 未改动任何裁决逻辑。**

> **本阶段代码边界**：`app.py` 布局 + `copy_cn.py` 文案。引擎四件套 `simulate.py` / `financials.py` / `game.py` / `calibration.py` **零改动**（沿用 P4.20 的边界）。

---

### C4. 首屏起点句 — 不给「未做出的决定」下裁决（`app.py` + `copy_cn.py`）

**病症**：一开屏，总标题写着「你在创造价值（+10.5%）」，紧接着下方 A 段红底写着「真正在亏钱：每投入 100 元，经营回报还比资金成本少 34 元」。同屏两句话直接打架。

**两层诊断**（第一层是表象，第二层才是病根）

1. **表象**：`app.py:838` 的总标题取博弈四态裁决（锚**象限单位经济**），而 A 段取自身财务（锚**该城基线财报**）。两套基数不同，符号本就可以相反——这是 handoff §六 已挂的已知简化。
2. **病根**：四个控件全在初始位时，用户**还没做任何决定**。此时给出「裁决」，等于替他背书一个他没做过的选择。矛盾只是这个错误的症状。

**改法**：首屏按 `_is_initial(k)` 分叉。

| 状态 | 首屏显示 |
|---|---|
| 四控件全初始位（定价 0 / 生态投资 0 / 联盟关 / 冲击 0） | `HEADLINE_BASELINE`：「起点：{city} · {quad}　还没有胜负——这是这座城、这个象限的**基线**。拨动下面任意一个控件，这里就会告诉你结果。」 |
| 任一控件动过 | 原裁决句；**且**两套口径符号打架时追加 `DUAL_BASIS_HINT` 一行小字 |

**`_is_initial` 只认四个「我的动作 + 环境」控件**，城市 / 象限 / 评估指标不计入——它们是「牌面」与「显示开关」，换城市 ≠ 做了决定（依据宪章 §6 的 2026-08 修订：动作预算只约束「我的动作」组）。

**`_sign_conflict` 取向「宁可不说，不可说错」**：任一侧缺失 / NaN（Phase 2 §C4 的 IC≤0 情形）/ 恰为零 → 一律判不冲突、不显示。`_as_float` 同时吃 `float` 与已格式化的 `"+10.5%"` 串。八种输入已单测。

**收益（一个改动解决三件事）**
- 起点态的矛盾**自然消失**——不是靠加解释，是靠**不说不该说的话**。
- 首屏从「一句用户看不懂的裁决」变成「一句告诉他该干什么的话」。原首屏无任何行动指引，撞宪章 §6 的 30 秒测试。
- 裁决句重新变得名副其实：它只对**你做过的事**发言。

> **修正了 Phase 4 的一个判断失误**：当年把 `DUAL_BASIS_NOTE` 定位为「放简报末尾与 About 页，不在首屏喧宾夺主」——逻辑上对，但没料到矛盾会在**首屏当场发生**。声明在第三屏、矛盾在第一屏，等于标注没盖住它要标注的地方。
> **教训：标注必须盖住它要标注的位置。** 「不喧宾夺主」是措辞长短问题，不是位置问题。

### C5. 板块 A 基准 ROE 行 — 入文案层并加注释

起点句刻意**不带数字**（切城市时标题只有城市名在变，视觉上像没反应；但把基线 ROE 塞进首屏会多一个数字 + 一个术语，且它是「牌面」不是「结果」，措辞稍偏就会被读成裁决）。

折中：把「换城市时到底什么在变」的解释放到 A 段基准 ROE 行——**数字与解释同处一行，不占首屏**。

```
基准 ROE：`-3.2%`　（该城基线财报。**换城市时变的就是它**——牌面不同，
起点就不同；象限只切换资本成本口径，不改这个数。）
```

同时该行从 `app.py:391` 硬编码**挪进 `copy_cn.SEC_A_BASE_NOTE`**。这是 Phase 2 §C5「全部可见文案走 copy_cn」时漏掉的一条，本次补齐。原括号内容（象限只切资本成本口径）保留——它防止用户误以为换象限会改基准数。

> **分工**：起点句管「你还没出牌」，基准 ROE 行管「你这副牌长什么样」。

### C6. 控制台 — 一键回到起点 + 卡片布局（`app.py` + `copy_cn.py`）

**① 「↺ 回到起点」按钮**
清空「我的动作」三旋钮 + 外生冲击（`k_price` / `k_eco` / `k_ally` / `k_shock`），**保留城市 / 象限 / 评估指标**——牌面与显示开关不是决策（P5.7）。同族于既有的「预设战略·一键试打法」：只替用户拨已有旋钮，不新增旋钮、不碰引擎，不撞宪章 §6 的 ≤3 动作预算。

**与起点句闭环**：清完四项后 `_is_initial` 恰好成立 → 首屏自动退回起点句。重置按钮与起点句是同一判据的两端。按钮本身也只在 `not _is_initial` 时出现——起点态点它没有意义，且它的出现本身即是「你做过决定」的信号。

> **踩过的坑**：首次实现放进「预设战略」`expander` 内，而该 expander 默认 `expanded=False` → **按钮存在但用户永远看不到**。折叠区里的东西等于没有。已挪至控制台底部常驻。

**② 三张动作卡等高**
滑块上方自带一行数值（≈1.5rem），`st.toggle` 没有 → 换电联盟卡矮一截。原注释写「靠卡片撑齐高度，比 CSS 微调稳」，但内容不等高时卡片撑不齐。修法：开关上方补 `1.5rem` 占位 div。

**③ `_is_initial` 位置**：原定义在 861 行、首个调用在 365 行。运行无碍，但读代码时会卡一下 → 挪到 `render_console` 之前。

**④ Streamlit 回调陷阱（本阶段暴露的 Phase 4 遗留 bug）**

> `st.session_state["k_price"] = ...` 写在 `if st.button(...)` 分支里会抛
> `StreamlitAPIException: cannot be modified after the widget with key k_price is instantiated`
> —— 滑块在本次运行中已实例化，之后就不许再写它的 key。
>
> **正解：走 `on_click` 回调**。回调在脚本重跑**之前**执行，此时写入合法，且回调自带 rerun，无需 `st.rerun()`。
>
> 此 bug 在 Phase 4 的「预设战略」按钮里**早已存在**，因该区默认折叠、无人点开点击而从未触发。
> Phase 5 把预设区改成常驻可见后当场炸出。新增的「回到起点」按钮原本照抄了同一写法，一并修正。
>
> **教训**：折叠区不只是「看不见」，它还会**掩盖那里的 bug**。把东西从 expander 里放出来时，要当作新代码重新验一遍。


### C7. 叙事层与诚实声明（`README.md` + `copy_cn.ABOUT_MD` + `IS_FALLBACK` 横幅）

**① `IS_FALLBACK` 降级横幅**（§D5 闭合）
显示在「参数恢复表」折叠区顶部——数字失真最要命的地方就在那张表。

> **踩过的坑**：`IS_FALLBACK` 是 `ensure_db()` **运行时**才写的模块级全局。
> 若写 `from ensure_db import IS_FALLBACK`，import 时就把 `False` 绑死，横幅**永远不会触发**。
> 必须 `import ensure_db as _edb`，调用后读 `_edb.IS_FALLBACK`。

**② About / 诚实声明收拢一页**（§D4 闭合）
原「关于 / 诚实声明」折叠区只有一句 `PROXY_NOTE_P3`，其余声明散落各处。现收拢为 `copy_cn.ABOUT_MD`，覆盖四块：数据是合成的（但参数可验证）· 已知简化四条 · 财务数字与 LLM 物理分离 · 用途声明。**未新开 tab**——沿用既有折叠区，不动两 tab 结构（P5.1）。

**③ `README.md` 正式稿**（§D3 闭合）
结构：一句话立意 → 30 秒五步路径 → 这是什么 → 因果链（mermaid）+ 三层架构 → 本地运行 + 仓库结构 → 已知简化 → 开发过程。

- 复用仓库现有资源：`viz2_recovery.png`（参数恢复图，作为"参数可验证"的直接证据）、`NEV_architecture.svg`、`phase4_rag_dataflow_offline_and_runtime.svg`。
- **数据表述纠正**：草拟阶段一度写成"基于真实的中国新能源汽车数据"——**错误且有害**。数据是 DGP 合成的（宪章 §4 明确排除"用真实车企数据标定参数"）。若面试官信了该表述，第一个问题必然是数据来源与清洗，而真相其实更强：**参数是埋进去再恢复出来的，所以可验证**。正确措辞为"合成数据，**锚定**真实企业的定性画像"——锚定 ≠ 基于。
- 文末指向五份审计日志的**决策演变台账**（"什么被推翻了、为什么"）。
### C8. `ensure_db` 降级路径实测（Colab · 全绿）

四个阶段以来 option B **从未被执行过**（走 `git add -f` 路线，主路径永不触发），
且 Phase 5 才刚把判据改严 —— **掉进降级路径的概率因此升高**，故必须实测。

| 场景 | 结果 |
|---|---|
| 四种坏文件（不存在 / 0 字节 / 空壳 0 表 / 非 sqlite） | 全部识别为不可用 ✓ |
| 正品 `nev.db` | 可用 ✓ |
| 删库 → 重建 | `usable=True`、`IS_FALLBACK=True` ✓ |
| 空壳（8192 B）→ 删档 → 重建 | 同上 ✓ |
| 重建库跑 `calibration.py` | β 4/4、γ 8/10 ✓ |

**重建库与仓库版逐项吻合**：六表行数 `sales 6570 · supply 1314 · macro 1095 · fin 72 · infra 72 · model 18` 完全相同；恢复出的 Shanghai 0.5657 / Xian 0.5513 与 `PHASE2 §G3`、handoff §六 记录的 0.566 / 0.551 逐位一致。
→ **反证 DGP 固定种子可复现，option B 重建的数据集与 force-add 的那份同批。**

> **Colab 上传的坑**：`files.upload()` 遇同名文件**不覆盖**，存成 `ensure_db (1).py`。
> 本次测试仍有效（仓库里已是新版），但若依赖上传来测未 push 的改动，会**静默测到旧代码**。
> 判别法：看输出里有没有新版才有的提示语。（顺带：那个副本别提交。）


---




---



### C9. 术语统一与措辞收敛（`copy_cn.py` + `app.py` 注释 + `README.md`）

**命名约定（本阶段冻结）**：分节叫「**板块 A / B / C**」，图叫「**图一 / 图二**」。
两套编号不同形，不会互相混淆。

清理的历史残留：
- `copy_cn.py:332` 一条注释写着「原图一/图二**改为**图 B/图 C」，但代码从未改 —— **改名被撤销、注释没跟着撤**，读代码的人会以为该叫图 B。已改写为约定说明 + 撤销原因。
- `app.py` 内 11 处「图 B / 图 C」（注释、docstring、模块头版面说明），其中 **两处是 `_t()` 的兜底字符串** —— 取不到 key 时会真的显示到界面上。全部统一。
- 「A 段 / B 段 / C 段」→「板块 A / B / C」。

**措辞收敛**：
- 「一把尺子」→「一个评估指标」。前者是口语，与界面正式说法不一致，属该扫的术语。
- `SCORER_HELP` 原写「三张图的纵轴会随之切换」→ 实为**图一 / 图二**两张。
  **行为本身正确、无需改动**：板块 A 是「天数 × 射线」，纵轴就是 ROE 与 spread 本身，
  评估指标在那里**结构上不存在可替换的槽位**；只有图一 / 图二是散点，纵轴才是可切换的。
  错的只是文案 —— 它承诺了一件产品不做、也不该做的事。属**文案与行为不符**，比措辞难听严重，故单列。
- 评估指标标签的长括号移入悬浮提示 —— 原括号把该列挤成两行、拖歪整排控件。
- 「起点」→「**初始状态**」，按钮「↺ 回到起点」→「↺ **回到初始**」。


### C10. 架构图更新（`NEV_architecture.svg`）· ERD 未动

**ERD 两张（`NEV_ER_chen.svg` / `NEV_ER_crowsfoot.svg`）：无需更新。**
Phase 5 未碰 schema，六张表与字段仍与 `nev.db` 一致（已对照 `ensure_db.REQUIRED_TABLES` 核验）。

**架构图六处过时 + 一处错别字：**

| 处 | 原 | 现 | 说明 |
|---|---|---|---|
| 时间轴 | `Phase 2 · 单企业绝对分析（本阶段）` | 去掉「（本阶段）」 | Phase 2 早已收官 |
| 时间轴 | 只画到 Phase 3 | 补一行 Phase 4 / Phase 5 | 画布 2480 → 2528，脚注下移 48 |
| Agent | `DeepSeek` | `glm-4-flash` | 实际用的模型（`config.py` 一行可换） |
| 尺子 | `四把可切换 reward 尺子：份额 / 利润 / EVA / 生态` | `价值创造 / 股东回报 / 资本回报（份额＝图一横轴）` | 份额已是图一横轴，不再作纵轴指标（`app.py:337`） |
| 尺子 | `存一次·切四次` | `存一次·换指标重看` | 同上 |
| 嵌入 | `.npy` | `vecs.npz` | 实际产物名 |
| **错别字** | 「选址**禅**赋」「区域**禅**赋」 | 「禀赋」 | 3 处。「禀赋」是本项目的核心概念之一，错字出现在架构图上尤其扎眼 |

> 尺子那两处值得注意：图上写的是 Phase 2 时代的设想（四把 reward），而 Phase 3 落地时份额改作图一横轴、纵轴只剩三个可切换指标。**图与实现脱节了两个阶段没人发现** —— 因为架构图平时没人回头看。
> 教训同 §C9：**没被使用的东西也不会被验证。**

---

## D. 待办 → 全部完成 (Phase 5 Deliverables)

| # | 事项 | 类型 | 状态 |
|---|---|---|---|
| 1 | ~~移动端：象限地图窄屏下退化~~ | 修 bug / 趣味 | ✅ 见 §C2 |
| 2 | **演示路径脚本**：五步、逐句对照实机核验完毕 | 叙事 | ✅ 已并入 `README.md` |
| 3 | README 正式稿（含 mermaid 因果链 + 复用现有架构图） | 叙事 | ✅ 见 §C7③ |
| 4 | About / 诚实声明收拢一页（`copy_cn.ABOUT_MD`） | 叙事 | ✅ 见 §C7② |
| 5 | `app.py` 接 `IS_FALLBACK` 提示条 | 修 bug 配套 | ✅ 见 §C7① |
| 6 | 术语统一：板块 A/B/C · 图一/图二 · 「尺子」→「评估指标」 | 文案 | ✅ 见 §C9 |
| 7 | 架构图更新（ERD 无需动） | 文档 | ✅ 见 §C10 |

---

## E. 验收自检清单 (Acceptance Checks)

- [x] `db_usable()` 五种情形判定正确（不存在 / 0 字节 / 空壳 / 非 sqlite / 健康）
- [x] 删库后走 option B 重建，`calibration.py` 随后跑通（Colab 实测，见 §C8）
- [x] `IS_FALLBACK` 在两种降级场景下均为 `True`（删库 / 空壳）
- [x] 实机：预设按钮 / 回到初始按钮均可用，初始态标题与裁决句正确切换
- [~] 横幅 UI 渲染未单独强制触发（逻辑已验，两种降级场景下 `IS_FALLBACK` 均为 True）
- [x] 象限地图 375px 竖屏保持 2×2、四格等高、横轴文字不裁切
- [x] 桌面端横轴线与卡片不重叠（**任意**高亮象限下均验）
- [x] 移动端「沙盘」tab 通查：图表一张一行、滑块一指标一行，判定可接受
- [x] `_is_initial` 六种控件组合判定正确（全初始位 / 四种单动 / 负向冲击）
- [x] `_sign_conflict` 八种输入正确（含 NaN / None / 零 / 格式化串）
- [x] 实机：起点态显示起点句；拨定价 → 换成裁决句（常州·Q1·−30% → 「独赢群输」）
- [x] 实机：切城市时基准 ROE 行数字随之变化
- [x] 五步演示路径逐句对照实机核验（量程 / 图序 / 读数 / 裁决态 / 象限选型）
- [x] 找没有金融背景的人实测 30 秒路径 —— **已测通过**
- [x] 宪章 §9 四问全绿：自洽 / 能演示 / 能迁移 / 好玩（见 §I）

---

## F. 待确认 / 遗留 (Open Items)

**本阶段闭合**
- ✅ **`generate_data.py` 真实入口**（Phase 1 §G2，挂四阶段）→ 真名 `build()`；另修其不落盘问题，见 §C1②。
- ✅ **`ensure_db()` 判据**（Phase 4 §G8）→ 改为"存在且可用"，见 §C1①。

**继续挂（诚实标注，不动）**
- **税 bug**（数值层）：`generate_data.py:233` `net = (ebit − interest)·(1 − tax)` 对亏损也打八折。只脏 `net_income`→ROE，**不脏 spread**。见 §P5.5，本阶段明确不修。
- **γ 8/10**：Shanghai / Xian 两区 CI 未覆盖真值。R² 健康、象限序数正确，属数值层。β 4/4 全绿。
- **两套 spread 口径**：`financials` 锚该城基线财报、`game` 锚象限单位经济，绝对符号可相反。**口径本身未动**（改 `game.py` 会推翻六态裁决定义，撞变更闸门）；Phase 5 只补齐了标注位置——起点态不再下裁决、动作后符号打架时首屏即时点破（§C4）。诚实标注现已覆盖矛盾发生的位置。
- **文风套话**：`glm-4-flash` 小模型上限，不再调提示词。
- **schema 中已有、引擎未接入的字段**：自研分 / 换电网络 / BOM 成本链，**有意不接**。RAG 可作叙事素材，不得据此改动引擎数字。

---

## G. 环境与复现 (Reproducibility)

沿用 Phase 4：Colab（Python 3.11，与 Cloud 对齐）。依赖未变。

```bash
python ensure_db.py       # 验证自举：打印 usable / fallback
python calibration.py     # 参数恢复 → simulation_config.json
streamlit run app.py      # 两 tab 沙盘
```

- **Key 处理规则**：运行期 Key 存 `st.session_state`、`type="password"`、不落盘不进 Git；构建期 Key 走环境变量 + `getpass`，不硬编码进任何文件。
- **Colab 注意**：改过文件后必须 `importlib.reload(module)`。

---

## I. 收官 · 宪章 §9 四问

| 问 | 判据 | 状态 |
|---|---|---|
| **自洽** | 因果链跑通、恒等式收口、不崩溃 | ✅ 引擎四件套零改动；新增 `_is_initial` / `_sign_conflict` / `_set_knobs` 均有单测；`ensure_db` 降级路径 Colab 实测全绿 |
| **能演示** | 公网可点、30 秒看懂 | ✅ 五步演示路径逐句对照实机核验；README 正式稿含路径表 + 参数恢复图 |
| **能迁移** | 核心机制过 §5 迁移测试 | ✅ 本阶段只动显示层与文案层，基座未被 NEV 概念污染；换皮仍只需替换 `copy_cn` 与微调层 |
| **好玩** | 没有金融背景的人愿意拨两下 | ✅ 已找无金融背景者实测 30 秒路径，通过 |

四问全绿 → **Phase 5 收官，打 release `v1.0`**。

> 打 `v1.0` 而非 `v0.6`：前五阶段是「在建」，Phase 5 收尾意味着这个东西**完整了** ——
> 五阶段全收、公网可用、README 齐备、诚实声明到位。0.x 暗示尚未成型，而它已经成型。
