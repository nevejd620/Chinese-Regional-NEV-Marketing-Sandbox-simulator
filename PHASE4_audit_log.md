# Phase 4 · Audit Log — 合页 + Action-triggered RAG + 商业分析简报

> **状态**: ✅ 已收官 · 待打 tag `v0.5`
> **在线**: https://chinese-regional-nev-marketing-sandbox-simulator-3lpxhv6sebdda.streamlit.app/
> **范围**: `triggers.py`(新增) + `build_corpus.py`(新增·构建期) + `make_cache.py`(新增·构建期) + `brief.py`(新增·运行期) + `corpus/`(新增·语料与向量) + `app.py`(合页 + C 段 + 仪表盘视觉) + `copy_cn.py`(追加) + `config.py`(追加) + `.streamlit/config.toml`(改主题) + `requirements.txt`(+openai/python-docx)
> **计量**: 全表人民币 (RMB)，中国市场口径（沿用 Phase 0–3）
> **上游依赖**: Phase 0–3 全部产出；引擎四件套 `simulate.py`/`financials.py`/`game.py`/`calibration.py` **一行未改**
> **核心兑现**: 给已经算出来的数字配一段**有据可依的解释**——且**LLM 从头到尾够不到财务数字**

本文件记录 Phase 4 的**设计冻结、被推翻的决策、红线的工程化落地、实测诊断、验收自检与 open items**。
Phase 4 是全项目唯一带 LLM 的阶段，也是宪章标注"最易漂移"的阶段，故本日志对**踩过的坑**记录得比前几阶段更细——它们才是这一阶段真正的经验。

---

## A. 已冻结的设计决策 (Frozen Decisions)

| # | 决策 | 依据 | 状态 |
|---|---|---|---|
| P4.1 | **两条通道物理分离**：数字走 `引擎 → 定值字符串 → 模板占位符`，文字走 `检索 → LLM`，二者只在渲染层汇合 | 头号红线：够不到就编不出 | ✅ 冻结 |
| P4.2 | 送进 prompt 的只有**定性标签**（裁决态/档位/名次），**不含任何数值** | 同上，红线的第一道闸 | ✅ 冻结 |
| P4.3 | **出口对账**：数字白名单 + 口算词黑名单，不合格的句子**丢弃并回退模板句**，不是给警告 | prompt 约束是 best effort，对账是机械保证 | ✅ 冻结 |
| P4.4 | 白名单按**渲染后的字符串**建（`-5.2%` 而非 `-0.0517`），精度在 `format_readouts` 一次定死 | 否则 LLM 复述 `-5.2%` 而白名单是原始浮点，对账全部误杀 | ✅ 冻结 |
| P4.5 | 简报 §二（逐项动作 + 杜邦）**全部由模板生成，零 LLM** | 这是数字最密的一章，正好完全锁死 | ✅ 冻结 |
| P4.6 | **向量全部离线预算**：语料与 query 均在构建期嵌入，产物随仓库走；运行期只做一次矩阵点积 | 运行期不装嵌入模型（Cloud 内存零压力）；用户自带的是 generation key，本就没有 embedding 权限 | ✅ 冻结 |
| P4.7 | 不上向量数据库（numpy 点积即可）、不做 cross-encoder 重排 | 宪章"别加基础设施"；31 条语料取 top-3，重排改善不了什么却要背几百 MB | ✅ 冻结 |
| P4.8 | **检索判据 = 结构化元数据优先、语义补位**：strategy 先按裁决态与象限**锚定**两条，再由相似度补第三条 | 纯相似度会让泛化性强的切片成为枢纽（见 §B2 实测） | ✅ 冻结 |
| P4.9 | 两个视角**按来源分池**：policy 只在 `policy.md` 检索，strategy 只在 `strategy.md` | 硬隔离，保证 6 条素材不重复（实测交集 0/120） | ✅ 冻结 |
| P4.10 | **框三只读、不接受追问**（无聊天框） | 追问必然引出"这个数字怎么算的"，模型手里没有算式只能编；且撞宪章"非自由聊天框、由动作触发" | ✅ 冻结 |
| P4.11 | 屏上与 docx **同源同字**：同一份 rep 渲染两次，**导出绝不二次调用 LLM** | 否则看到的 ≠ 下载到的，是演示事故 | ✅ 冻结 |
| P4.12 | 屏上 = 两段无大标题（①数字复述 ②影响与策略）；docx = 四章完整版 | "算出来的"与"解释出来的"各占一段，读者一眼分得清 | ✅ 冻结 |
| P4.13 | **四条降级路径**：无 Key→演示/模板版；调用失败→只出引擎读数版；某段被对账清空→该段回退模板；检索缺产物→空材料仍出简报 | 任何情况下简报都出得来，断网可演示 | ✅ 冻结 |
| P4.14 | `trigger_key = 象限\|裁决态`，**旋钮档位不进检索键** | 档位只决定"讲得多重"（由定值字符串带过去），不决定"讲什么道理"；少一维 ≈ 少 3 倍语料压力 | ✅ 冻结 |
| P4.15 | 供应商无关：`config.LLM_BASE_URL/LLM_MODEL/EMBED_MODEL` 三行决定一切，走 OpenAI 兼容接口 | 国内平台（智谱/百炼/混元/千帆）均兼容；换厂商不改代码 | ✅ 冻结 |
| P4.16 | **合页**：原 P2/P3 两个 tab 合并为一页「沙盘」，全站两 tab | 见 §B1（消除了两根互不相通的定价滑块） | ✅ 冻结 |
| P4.17 | 滑块预算修订：≤3 **只约束"我的动作"组**；牌面/环境/显示开关不计入 | 预算本意是"决策空间别爆炸"，非"屏幕上别有控件"；P3.10 已是同一判例 | ✅ 冻结（宪章 §6/§10.2 同步修订） |
| P4.18 | **配色四分**：绿=可操作／象限色=你是谁／石墨灰=别人／红=零轴 | 交互色与内容色不共用色相，否则用户分不清"控件"还是"数据" | ✅ 冻结 |
| P4.19 | 四象限色板**单一真相源**在 `copy_cn.QUAD_COLOR`，象限地图与图一/图二共用 | 此前两处各写一套，卡片色与散点色对不上号 | ✅ 冻结 |
| P4.20 | 引擎四件套零改动，Phase 4 只做下游消费者 | 沿用 `financials.py` 当年对引擎的关系 | ✅ 冻结 |
| P4.21 | **演示缓存只存 json、不存 docx**；docx 由 `to_docx(rep)` 在用户点下载时现场渲染 | 渲染是纯确定性的，同一 json 永远出同一文档 → 预存冗余；且改版式无需重跑缓存 | ✅ 冻结 |
| P4.22 | 缓存的每个数字**必须由引擎算出**（`make_cache.py` 完整复刻 app 取数链路），严禁手填占位值 | 缓存是给**没填 Key 的用户**看的；页面上方图表是引擎真值，简报若是另一套数字，红线当场破在最显眼的免费路径上 | ✅ 冻结 |

---

## B. 决策演变台账（被推翻/撤销的，务必留档）

### B1. 合页：三 tab → 两 tab
原设计拟为简报新开第四个 tab。**推翻**——用户指出这是"顺着已有结构堆"。合页后暴露并修掉一处**真实混乱**：

> P2 有「自主定价变动」滑块、P3 有「定价」滑块，**同一件事、两根滑块、互不相通**：拨了 P2 的定价，跑到 P3 发现价格又回原样。分居两 tab 时无人追问，合成一页立刻站不住。

连带闭合：原 P2「需求侧位移」由 P3「生态投资」正式接管（闭合 `PHASE1_audit_log` §G4 挂了两阶段的 open item）；原 P3「象限跟随 Phase 2」开关删除（一页只有一个象限选择器，天然同步）。**净减两个控件，动作旋钮仍是 3 个。合页全程是减法。**

### B2. trigger v1 → v2：检索坍缩的实测与修正
v1 用 `象限 × 裁决态` 作两个视角共同的检索键。首轮构建诊断打脸：

| 指标 | v1 实测 | v2 实测 |
|---|---|---|
| 裁决态命中 | `CREATE_TRAIL`/`WIN_ALONE`/`MUTUAL_DESTROY` 三条切片**一次都没被选中** | **120/120** |
| 城市材料 | 选西安却检索到柳州、上海的材料 | 抽样四组**全部命中本城** |
| 枢纽切片 | 「柳州：上游资源短板」在 20 格里出现 **17 次** | 消失 |
| 语料利用率 | 17/31 | **24/31** |

**根因**：query 里象限词二十余字、裁决态词十余字，象限词在向量空间里压倒裁决态；且 policy 语料多按城市组织，而 v1 的键里根本没有城市。
**修正**：policy 视角改用 `城市 × 裁决态`（30 条 query）；strategy 保持 `象限 × 裁决态`（20 条），并改为**锚定 + 补位**（`strategy.md` 的 9 条恰是 5 裁决态 + 4 象限，与 trigger 两维一一对应，不必靠相似度去猜）。

> **留给后人的判据**：不要拍脑袋定 trigger 数，**让诊断脚本告诉你有效分辨率是多少**。120 种组合 → 90 组不同结果，才说明格子之间真的取到了不同材料。

### B3. 主体漂移：改了三次才钉住
简报的分析主体是「区域 × 象限的代表性企业」（P2.12 冻结），但模型三次跑偏：

| 轮次 | 症状 | 措施 |
|---|---|---|
| 1 | 「**西安**在极致性价比象限中保持份额领先」——把城市当分析对象 | 系统提示中段加"分析主体"说明 |
| 2 | 仍写「西安应重视价值创造」 | 首尾各钉一次（小模型对开头结尾最敏感，中段易被稀释） |
| 3 | 检索接通后改写「**西安新能源汽车产业**面临零部件配套不足」——顺着政策材料的口吻写产业分析 | ①给参考材料**声明身份**（"是这家企业所在地的背景条件，不是分析对象"）②给**正误示例**（用上一轮的真实错误输出做反例）③城市标签降格为「所在地（区位背景，非分析对象）」 |

**结论**：城市**必须保留**（它是禀赋，决定 γ 与基线财报；且 policy 语料半数按城市组织），但要**降格为区位背景**。对小模型，**具体示例 > 抽象指令**。

### B4. 其他被推翻/撤销的
| 议题 | 演变 | 最终 |
|---|---|---|
| 两段式 = 两次 API 调用 | §① 读数复述本就是纯 python 渲染，不需要 LLM | **一次调用** |
| 48 格 trigger × LLM 改写 query | 改写层整个砍掉，query 由模板拼 | 50 条模板 query，构建期一次性嵌入 |
| Baseline vs RAG 对照、检索评估（Assignment 2 的 Step 7/9） | 属作业评审环节，沙盘无此观众 | **砍**（但**出口对账不可砍**——它形似评估、实为红线的执行机构） |
| 48 份演示缓存全预生成 | 每改一次 prompt/语料/模板即全部作废，且淹没提交记录 | **固定 3–4 条演示路径** |
| 缓存用手填的示例数字 | 初版用占位值（`share=0.2` 等）快速跑通。**推翻**——缓存面向没填 Key 的用户，与页面上方图表并列展示，数字对不上等于**当众破红线** | 新增 `make_cache.py`，走 `simulate → financials → game` 取真值 |
| 「象限跟随城市」自动联动 | 我用 `baseline[city]["quadrant"]` 驱动 UI —— **它是该城基线企业碰巧所处的象限（数值层的一条记录），不是城市的属性**。此举在界面上重新焊死了 Phase 3 §B 刚拆掉的「城市 ⊥ 象限解耦」 | **整体撤除**（连同 `QUAD_OFF_HOME` 提示）。教训已写进 `app.py` 文件头注释防复发 |
| 「定价基线」的 tooltip 与诚实声明 | 查证发现两台引擎各锚各的起点，遂写进 tooltip → 诚实声明 → UI 行为 → 结构性约束。但实测比值 0.86–1.03，**默认状态下没有可察觉的差异** | **整体删除**。这是宪章 §7「把观众当审稿人」的典型：为一个不存在的问题写了四层脚注 |
| 主色 蓝 `#185FA5` → 绿 `#12A47A` | 过宪章 §8 三问：非修 bug、非加精度，属"提升趣味/直觉" | 改。并连带把 Q3 改墨绿、B 段改石墨灰（见 §B5） |

### B5. 配色：主色与内容色抢意义
初版主色绿 `#12A47A` 与 Q3 垂直整合的绿 `#0E9F6E` 几乎同色系，B 段色带的青也贴近 Q1。**用户看到一个绿色的东西，分不清它是"可以点的控件"还是"Q3 象限的数据"。**
候选方案：①改蓝主色 ②主色留绿、让开冲突的内容色 ③不动。
**选 ②**：Q3 → 墨绿 `#0A6E4C`，B 段 → 石墨灰 `#4A5B63`。绿色只属于"可操作的东西"，语义留住、混淆解决。

> **这条是通用规则，不限 NEV**：交互色与内容色不共用色相。换皮到快消/旅游时照样成立。

---

## C. 红线的工程化落地 (How the Red Line Is Enforced)

宪章与交接文档都写着"LLM 不得计算、推算、修正任何数字"。但**写在文档里不算落地**，Phase 4 把它做成了四道机械保证：

1. **够不到**：`Readout.labels()` 进 prompt，`Readout.values()` 不进。模型看到的是「定价动作：明显下调」「价值利差：为负」，看不到 `-12%` 与 `-5.2%`。
2. **不产出**：LLM 返回的 JSON 里只有措辞；数字由 python 填模板。简报 §一（屏上）与 §二（docx）完全由模板渲染。
3. **数字白名单**：正则扫出成文里每个数字，逐个比对引擎读数（按渲染后形态）。
4. **口算词黑名单**：拦截"腰斩/翻倍/高出三成/减半"这类**没有阿拉伯数字、实质在做算术**的表达——这是纯 prompt 约束抓不到的漏洞。

不合格的句子**直接丢弃并回退模板句**（宁可少一句，不可脏一个数），丢弃记录在 UI 上以折叠形式**如实展示**（宪章 §2 目标③ 诚实可审计）。

**冒烟实测**（故意投喂三句问题文本）：

| 句子 | 判定 |
|---|---|
| 价值利差为 −5.2%，份额 38% | ✅ 保留（两数均在白名单） |
| 利差较上轮**腰斩** | ❌ 丢弃 · 口算式表达 |
| 回报率高达 **−9.9%** | ❌ 丢弃 · 数字不在白名单 |

**真实生成实测**：`dropped` 为空——说明 prompt 约束已足够强，对账是第二道保险而非日常拦截器。

---

## D. 数据流与产物 (Pipeline & Artifacts)

```
构建期（作者本地跑一次，产物进 Git）
  corpus/policy.md · strategy.md  →  切片 → 嵌入 → chunks.jsonl + vecs.npz
  triggers.QUERIES（50 条）        →  嵌入 → vecs.npz(query_vecs)
  make_cache.PRESETS（3–4 条）     →  引擎取真值 → 走完整管线 → cache/*.json

运行期（用户点「生成报告」，一次 API 调用）
  ① 触发  旋钮 + 引擎输出 → trigger_key + Readout
  ② 检索  查表取 query 向量 → 分池点积 → policy 3 条 + strategy 3 条
  ③ 生成  定性标签 + 切片 → LLM → JSON（只有措辞）
     数字通道并行：Readout → 定值字符串 + 白名单（不经过 LLM）
  ④ 对账  数字白名单 + 口算词黑名单 → 不合格则丢弃回退
  ⑤ 渲染  同一份 rep → 屏上 markdown（两段） + docx（四章两表）
```

| 产物 | 体积 | 进 Git | 理由 |
|---|---|---|---|
| `corpus/chunks.jsonl` | ~22 KB | ✅ | 切片正文与出处元数据 |
| `corpus/vecs.npz` | ~538 KB | ✅ | 语料 31×2048 + query 50×2048。**Cloud 上没有 embedding key，重算不了** |
| `cache/*.json` | 小 | ✅ | 3–4 条演示路径，断网可演示。**只有 json，无 docx**（见 P4.21）；数字由 `make_cache.py` 走引擎算出（见 P4.22） |
| `simulation_config.json` | — | ❌ | 与上相反：Cloud 能自己现算（`calibration.py`） |

> **对照记忆**：`simulation_config.json` 不入库因为**算得出来**；`vecs.npz` 必须入库因为**算不出来**（缺 key）。判据是"部署环境能否自力再生"，不是"是不是产物"。

---

## E. 语料 (Corpus)

| 文件 | 条数 | 组织维度 | 用途 |
|---|---|---|---|
| `corpus/policy.md` | 22 | 六城各 2–3 条（禀赋 + 局限）+ 全国性政策 8 条 | 简报 §3.1 政策与区位视角 |
| `corpus/strategy.md` | 9 | 5 裁决态 + 4 象限，与 trigger 两维**一一对应** | 简报 §3.2 竞争与生态视角 |

**切片规范**：每条 100–320 字、语义自足；`##` 一级 / `###` 二级两级标题均可切；末行 `（来源：…）` 抽为元数据，一行多来源以 `；` 拆分并去重（现 23 个来源）。
**刻意避开数字密集段落**：语料里的数字越少，与引擎读数错配的风险越低——出口对账能拦"编造"，拦不住"把补贴 12% 说成利差 12%"。据此把购置税那条从具体百分比改写为**机制描述**。

**切片器踩坑**：`"###".startswith("##")` 为真 → 必须先判 `###`，否则子标题会被当成一级标题、正文落空。
**锚点解析踩坑**：`"CREATE_TRAIL".startswith("CREATE")` 为真 → 状态匹配必须**按长度降序**，否则 `CREATE_TRAIL` 被 `CREATE` 吃掉（干跑自检报 `裁决态锚点 4/5` 才发现）。

**闲置 7 条**（利用率 24/31）：3 条系同主题次优选择（合理备份）；4 条（购置税、补能设施×2、置换政策）属**需求侧与基础设施**主题，与 policy query 的"区域禀赋"主线语义不搭。**不予处理**——"每条材料都必须被用上"是伪目标，真实语料库闲置率远高于 23%。

---

## F. 验收自检清单 (Acceptance Checks)

### F1. 构建期
- [x] 切片 31 条（policy 22 + strategy 9），0 条待留意
- [x] 锚点覆盖：裁决态 **5/5**、象限 **4/4**、六城各有专属材料
- [x] 嵌入 31 语料 + 50 query，写出 `chunks.jsonl` + `vecs.npz`
- [x] 有效分辨率 **120 种组合 → 90 组不同结果**（健康）
- [x] `make_cache.py` 生成演示缓存，读数全部来自引擎（`--dry-run` 可单独校验取数链路）
- [x] 缓存直出：`load_cache → to_markdown / to_docx` 不调 API、不调模型
- [x] 裁决态命中 **120/120**（锚定生效）
- [x] 视角隔离 **0/120**（分池检索，预期为 0）

### F2. 运行期与红线
- [x] `live` 模式跑通（智谱 `glm-4-flash`，一次调用）
- [x] 检索命中锚点：`WIN_ALONE` + `Q4` 两条如期入选
- [x] 分析主体正确（主语「该企业」，城市退为区位状语）
- [x] 出口对账：冒烟三句中 2 句被正确丢弃；真实输出 `dropped` 为空、无误杀
- [x] 简报中每个数字均可在引擎输出逐字对上
- [x] 四条降级路径：无 Key / 调用失败 / 段落被清空 / 缺检索产物，均能出简报

### F3. 输出与文案
- [x] 屏上两段（数字段 + 解释段），无章节大标题
- [x] docx 四章 + 表 1/表 2（题注在上、单元格水平垂直居中）+ 引用另起一页
- [x] 字体：宋体（`w:eastAsia`）+ Calibri/Arial；一级 16pt / 二级 14pt / 三级 12pt；正文两端对齐 + 首行缩进 2 字符
- [x] 屏上与 docx 同源同字，导出不二次调用 LLM
- [x] 全站可见文案走 `copy_cn`；50 个直接引用键 + 全部 `_t` 兜底键无缺失

### F4. 合页与视觉
- [x] 两 tab；定价滑块合一；动作旋钮仍为 3
- [x] 象限地图：真十字坐标轴、四格等高、轴标题移出图区、当前象限强高亮
- [x] 四象限色板单一真相源，地图卡片与图二散点一一对应
- [x] 「你」在图一/图二/地图三处同色；对手统一石墨灰
- [x] 控件在上、名称在下 → 三个动作顶端天然齐平（不依赖像素级微调）

> 收官：§F1–F4 全绿，宪章 §9 四问（自洽/能演示/能迁移/好玩）通过。待 push + 打 tag `v0.5`。

---

## G. 待确认 / 遗留 (Open Items)

1. **文风套话**：`glm-4-flash` 仍会写「实现可持续发展」这类空话，prompt 已明令禁止但压不住（小模型能力上限）。**不再调 prompt**——再加约束边际收益极低，且撞宪章 §7「把观众当审稿人」。日后如在意，两条现成路：换 `glm-4-plus`（改 `config.py` 一行），或在渲染层加套话词表后置清洗。
2. **`_ARITH_WORDS` 潜在误伤**：「增加了」「减少了」在纯定性语境下合法（"增加了投入"）。现按严格跑，真实输出未出现误杀；若日后频繁丢句再放宽。
3. **`LLM_MAX_TOKENS` 实测教训**：初设 2000 会把五段中文的 JSON 截断在半截，且 `finish_reason` 报 `stop` 具有误导性。现为 4000。换模型时需重验。
4. **JSON 解析三级兜底**：GLM 常在 JSON 外裹说明文字或围栏，仅剥围栏不够。现为 直接解析 → 剥围栏 → 取最外层 `{...}`；并优先使用 `response_format={"type":"json_object"}`（不支持则自动退回）。
5. **Python 版本缝隙**：f-string 内表达式跨行需 ≥3.12（PEP 701），而 Cloud 钉 3.11 → 本地编译通过、线上 SyntaxError。已全库扫除。**此类问题本地验不出来**，记此备查。
6. **Phase 3 及以前的遗留继续挂**：税 bug（数值层，搭车下次 Phase 0 重跑修）· γ 8/10（Shanghai/Xian）· schema 中自研分/换电网络/BOM 成本链有意未接入引擎（Phase 4 的 RAG 可用作叙事素材，**不得据此改动引擎数字**）。
7. **`ensure_db()` 判据**：建议改为"存在**且有表**"（仓库内 `nev.db` 可能为空壳）。属修 bug、边际成本近零。
8. **移动端**：象限地图用 `st.columns([1, 0.06, 1])` 夹中轴线，窄屏下中间列会被挤没。桌面端无碍。
9. **API Key 安全**：Key 存 `st.session_state`、`type="password"`、不落盘不进 Git。构建期 key 走环境变量 + `getpass`，**不得写进 notebook**（`Individual_Assignment2.ipynb` 里那把硬编码的 Anthropic key 应吊销重发）。

---

## H. 环境与复现 (Reproducibility)

- 开发：Google Colab（与 Cloud 对齐，Python 3.11）。
- 依赖：`pandas>=2.2 numpy statsmodels plotly streamlit scipy openai>=1.0 python-docx>=1.1`。
- **运行期不依赖任何向量栈**（无 sentence-transformers / faiss / chromadb）。

**构建期（改语料后需重跑，需 embedding key）**
```bash
python build_corpus.py --dry-run     # 只切片与质量自检，不花额度
export ZHIPU_API_KEY=xxxx            # Colab: os.environ[...] = getpass(...)
python build_corpus.py               # 切片 → 嵌入 → 写 npz → 三项诊断
```

**演示缓存（改 prompt / 语料 / 简报模板后需重跑，需 generation key）**
```bash
python make_cache.py --dry-run   # 只算引擎读数、不调 LLM，校验取数链路
python make_cache.py             # 走完整管线 → cache/*.json
```

**运行期**
```bash
python brief.py           # 简报冒烟（检索 + 对账 + 降级 + docx）
streamlit run app.py      # 两 tab 沙盘
```

- 改文案：只动 `copy_cn.py`，热重载生效，**不重跑**。
- 改配色：`copy_cn.QUAD_COLOR` / `SECTION_COLOR` + `.streamlit/config.toml`。
- 换 LLM 供应商：`config.py` 三行（`LLM_BASE_URL` / `LLM_MODEL` / `EMBED_MODEL`）。
- **Colab 注意**：改过文件后必须 `importlib.reload(module)`，否则用的仍是缓存的旧模块（本阶段两次因此误判"改动没生效"）。
