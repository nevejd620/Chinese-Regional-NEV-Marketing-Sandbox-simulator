# 项目宪章 · 深度与范围护栏 (Scope Charter)

> **这份文件的唯一用途**: 每当你或代码想"为了更真实/更精准/更细节"回头改前半部分时,先读它。
> **它不是路线图**(路线图讲"做什么"),它是**刹车片**(讲"到此为止、别再往下钻")。
> **判决权**: 本文件与任何"让它更真实"的冲动冲突时,以本文件为准。

---

## 0. 一句话立意 (What this actually is)

这是一个**分析框架的作品**,不是一个新能源汽车预测器。

它坐落在 Technologies ∩ Decision Making ∩ Quantitative Methods 的交集(即"商业分析"的学科核心),用来**展示一种能力**:把宏观/微观经济、金融、投资、博弈论、会计财务分析(本科)+ 商业分析与编程(硕士)**整合成一套自洽的因果框架,并用代码把它做成人人能玩的东西**。

> 艺术生有毕设,这是我的毕设。检验的是**框架设计 + 技术实现**,不是对某个行业的实证精度。

### 0.1 核心心智模型 · Foundation × Fine-tuning

> **这个项目 = 基座模型 (foundation model);新能源汽车 = 在基座上的一次微调 (fine-tuning)。**

- **基座层(foundation)**: 通用的分析框架与架构——三层数据、双维定位、确定性引擎、多把尺子、RAG 简报,以及"禀赋×定位×旋钮→后果→多尺子→裁决"这条因果骨架。**这层要通用、要干净,是真正要保护和打磨的资产。**
- **微调层(fine-tuning)**: 新能源汽车这一具体实例——四象限、区域禀赋、锂价冲击、电池自研等等。**这层本来就该 specific、有细节、有血有肉。** 它是基座能力的一次示范,不是负担。

**这个心智模型改变了"可迁移"的含义**: 迁移到快消/旅游/农产品/就业时,换掉的是**微调层**(整套 NEV 内容),保留和复用的是**基座层**。所以约束的重点是"**别让 NEV 的细节渗进基座、把基座焊死**",而**不是**"别把 NEV 做得太具体"。NEV 尽管做得饱满——只要基座保持干净可拆卸。

---

## 1. 三条立意(宪法序言,不可谈判)

一切下游规则都从这三条推导。任何改动若违背其一,自动否决。

1. **这是"实用技术知识"的毕设。** 目的是**呈现整合与实现能力**,不是产出一个学术级精确的行业模型。
2. **框架 > 精度。** 我不懂新能源汽车的细节,也**不打算为了足够真实而大改框架**。要给面试官看的是"系统分析 + 编程呈现"的能力,不是参数标得多准。
3. **第一观众是来玩的人,不只是面试官。** 要发到社媒,让大家**玩一玩、have fun**。所以重框架与互动逻辑,**不堆砌术语**。这套**基座**之后要迁移到 快消 / 旅游 / 农产品 / 就业 + 新能源 = **5 个领域**——但迁移的是**整体框架(基座)**,不是每个机制;NEV 这一层(微调)可以、也应该 specific,只要**基座保持干净、可拆卸**即可。

---

## 2. 目标函数 · 优先级排序 (Objective, ranked)

当两个目标打架,永远让位给排序更高的那个。**精度排在最后,且永不值得为它牺牲框架。**

| 优先级 | 目标 | 含义 |
|---|---|---|
| ① 最高 | **框架清晰与自洽** | 因果链讲得通、恒等式收口、解耦干净。架构(基座)是主角。 |
| ② | **互动趣味与直觉** | 30 秒上手、像游戏、拨一下有反馈。 |
| ③ | **诚实可审计** | 简化但不欺骗,proxy 如实标注。 |
| ④ | **基座可拆卸(低权重)** | 只要求**基座层**不被 NEV 焊死;**不要求**每个 NEV 机制都通用。这是"别把地基浇死"的底线,不是主动优化项。 |
| ⑤ 最低 | **数值精准 / 真实标定** | 够用即可(符合常识的设定值)。**永不为它回退框架。** |

> **关于 ④ 的降权说明**: 上一版把"跨领域可迁移"排在第 ③、且当成每个机制的硬测试,这**过头了**——会逼 NEV 变得没有血肉。现在降到第 ④、且只约束基座层。真正的迁移性其实是 ① 的**副产品**:基座只要干净,自然就能拆下来换皮,不需要为它单独较劲。

---

## 3. Granularity 契约 · 三层精度天花板

代码分三层,**每层的精度要求不同,越往下越松**。混淆层级、用"结构层的严谨"去要求"数值层",就是漂移的开始。

| 层 | 是什么 | 例子 | 精度要求 | 停手信号 |
|---|---|---|---|---|
| **结构层** | 因果链、会计恒等、解耦、参数可恢复 | 杜邦勾稽、ROIC−WACC、收入=P×Q、回归恢复 β/γ | **高** · 必须对、自洽 | 这是主角,**值得打磨** |
| **关系层** | 序数、相对强弱、方向 | 四象限可分、区域禀赋排序、降价→销量升 | **中** · 谁高谁低对、量级大致合理即可 | **序数对了就停** |
| **数值层** | 绝对参数、标定值 | β=−1.3、γ=0.35、集群指数=0.85 | **低** · 符合产业常识的**设定**即可,非实测 | 一旦想"标得更准"→**立即停** |

> **黄金判据**: 问自己"这个数字错了,框架会塌吗?"——不会塌,就属数值层,不许为它花时间。会塌,才是结构层,值得较真。

---

## 4. Scope 边界 · 明确排除清单 (Out of scope)

以下这些**很诱人、但都是陷阱**,一律排除。看到自己在做其中任何一项,就是走错了。

**明确在范围内**
- 三层数据架构(SQL/JSON/RAG)、双维定位(选址×象限)、确定性引擎、四把尺子、RAG 简报。
- 让每个机制**过迁移测试**(见 §5)。
- 让首屏是结论、让交互像游戏。

**明确排除(碰到即回头)**
- ❌ **用真实车企数据标定参数** —— 数值层只需常识设定,查真实数据是往坡上爬。
- ❌ **穷尽行业细节**(更多车型/更多成本项/更真实的税制财报) —— 框架不需要它。
- ❌ **为"更逼真"给博弈引擎堆动态/加耦合** —— 博弈引擎本身属**结构层**(§3)、是 Phase 3 主体,**允许做厚**;红线只是不为"让 spread/均衡更真实"去追加动态层次。〔修订(2026-08):早期"spread 保持静态·决策⑧"已在 Phase 2 被推翻——spread 走了动态路 B、随滑块穿零轴;本条遂由"禁动态博弈引擎"改写为"禁为精度堆动态"。**动态博弈引擎(Bertrand/Nash/Logit/慢层生态累积)现属明确在范围内**,是 Phase 3 的结构层主体,不再排除。〕
- ❌ **加更多滑块/变量求"全面"** —— 复杂度膨胀是趣味的敌人。
- ❌ **NEV 特有、不可迁移的机制** —— 过不了 §5 迁移测试就砍。
- ❌ **学术级严谨的稳健性检验** —— 这不是论文,是玩具。
- ❌ **为"面试官会质疑不够准"而返工** —— 观众是来玩的,不是审稿人。

---

## 5. 基座 vs 微调 · 分层测试 (Foundation vs Fine-tuning)

> **规则(已放宽)**: 测试**只针对基座层**,不针对每个 NEV 机制。问一句:"**这个东西属于基座,还是属于 NEV 微调?**"
> - 属**基座** → 它必须保持领域中性(能横着走完五列),被 NEV 概念污染就要清理。
> - 属**微调** → **随便 specific,不设限**。锂价、电池自研、四象限的具体命名……越饱满越好,只要它待在微调层、不往基座里渗。

**基座层 · 领域中性骨架(要保护)**

> 禀赋(给定牌面,不可调) × 定位(用户选,改写量程) × 旋钮(用户拨) → 确定性后果 → 多把尺子评分(会翻转) → 叙事裁决

这条骨架 + 三层数据架构(SQL/JSON/RAG)+ 确定性引擎的**接口形状**,是五域通用的。迁移=把下面这张表的"新能源汽车"列换成另一列:

| 基座槽位 | 新能源汽车(当前微调) | 快消 | 旅游 | 农产品 | 就业 |
|---|---|---|---|---|---|
| 禀赋 | 区域产业配套 | 渠道/区域市场 | 目的地资源 | 产区禀赋 | 地区/行业 |
| 定位 | 四象限战略 | 品类定位 | 客群定位 | 品类定位 | 职业定位 |
| 旋钮 | 定价/生态投资/联盟 | 定价/铺货/促销 | 定价/营销/合作 | 定价/品牌化/渠道 | 技能投资/求职策略 |
| 尺子(会翻转) | 份额/利润/EVA/生态 | 份额/利润/品牌/复购 | 客流/利润/口碑/稳定 | 销量/利润/溢价/稳定 | offer/薪资/成长/稳定 |

**微调层 · NEV 专属内容(不设限,尽管饱满)**: 锂价冲击、电池/智驾/芯片自研、γ 成本传导、九公司锚点、区域集群指数……这些**就该是 NEV 的血肉**,不需要"能迁移"。它们的存在恰恰是在证明基座能承载一个真实感的实例。

> **唯一的红线**: 别让微调层的概念(如"锂价""电池")**硬编码进基座的接口**。基座认的是"外生冲击""成本传导系数"这类**通用名**;"锂价"只是当前微调给它的一个**取值/别名**。做到这一点,换皮时基座一行不用动——这才是 foundation × fine-tuning 的真正收益。

> **命名范式(2026-08 补充,由上面这条红线推出)**: 一切**可见文案**统一写成「**通用名(NEV 别名)**」——主标签用通用名,括号或 tooltip 放 NEV 具体物。
> 已落地示例:「关键原材料价格冲击(锂价冲击)」·「非价格吸引力(aᵢ)」。
> 收益:换皮时主标签一字不改,括号里换成"(化肥价格冲击)"即可;且实现上只动 `copy_cn.py` 一个字符串,热重载生效、不触发任何重跑。

---

## 6. 趣味底线 · 游戏化护栏 (Playability floor)

呼应"看起来太恐怖了":每一处交互,过不了下面任一条就要简化。

- **30 秒测试**: 一个没有金融背景的人,能在 30 秒内看懂并拨动一次、且看懂反馈吗?不能 → 简化。
- **结论前置**: 首屏第一眼是**一句人话结论**,不是一排控件。
- **术语预算**: 首屏 + 简报里的专业术语总数 ≤ 5 个(其余全用大白话)。ROIC/EVA 这种必须出现时,旁边给一句"人话翻译"。
- **少即是好玩**: **「我的动作」旋钮 ≤ 3 个/领域**(维持现状)。想加第 4 个,先砍一个。
  〔修订(2026-08 · Phase 4 合页):原文写作"滑块 ≤3"。合页后控制台分两组,该上限**只约束"我的动作"组**(用户的决策变量:定价/生态投资/联盟);**牌面**(城市/象限)、**环境**(外生冲击)、**显示开关**(记分尺子)不计入预算,但各组内部同样应克制。依据:预算的本意是"决策空间别爆炸",不是"屏幕上别有控件";P3.10「记分尺子开关不占预算」已是同一判例的先例。〕

---

## 7. 反漂移红旗 · STOP 词表 (Drift red-flags)

这是本文件的**核心工具**。下面这些念头一冒出来,就是刹车信号——不是继续,是停手。

| 冒出的念头 | 它其实在说 | 正确反应 |
|---|---|---|
| "为了更真实,得查真实数据重标参数" | 想爬数值层的坡 | **停**。数值层只需常识设定。回去做框架/趣味。 |
| "再加一个变量/滑块会更全面" | 想加复杂度 | **停**。复杂度是趣味的敌人;想加第 4 个滑块先砍一个。 |
| "这个参数面试官会质疑不够准" | 把观众当审稿人 | 观众是来玩的。诚实标 proxy 即可,不追精度。 |
| "前面那段不够严谨,回去改" | 想为精度回退已冻结代码 | 过 §8 变更闸门;非 bug 不改。 |
| "把'锂价''电池'这类 NEV 词直接写进引擎/接口" | 想把微调概念焊进基座 | **停**。基座只认通用名(外生冲击/成本传导);NEV 词留在微调层做取值。(注意:NEV 机制本身**尽管加**,别渗进基座即可。) |
| "模拟得再逼真一点就完美了" | 把玩具误当预测器 | 立意②:框架 > 精度。这已经够了。 |

---

## 8. 变更闸门 · 动前序代码前必过 (Change gate)

> 任何要修改**已冻结阶段**(Phase 0/1,及已收官的 Phase 2 部分)代码的改动,必须三问全过,否则不动:

1. 这是**修 bug**,还是**加精度/加真实度**? —— 加精度/真实度 → **拒**。
2. 如果动的是**基座层**代码:它会不会把 NEV 概念**焊进基座**、让以后换皮更难? —— 会 → **拒**。(动**微调层**代码不受此问约束。)
3. 它让框架**更清晰、或更好玩**了吗? —— 否 → **拒**。

**只有三种改动可以碰前序代码**: 修 bug · 提升框架清晰度(含给基座解耦、去污染)· 提升趣味/直觉。其余一律记进"未来阶段"占位,不回退。

---

## 9. "够好了"的定义 (Definition of good enough)

> 每个 Phase 收官,**不看"够不够真",看下面四问是否全绿**。全绿就打 tag、往前走,不回头。

- [ ] **自洽**: 因果链跑通、恒等式收口、不崩溃。
- [ ] **能演示**: 公网可点、30 秒看懂。
- [ ] **能迁移**: 核心机制过 §5 迁移测试。
- [ ] **好玩**: 没有金融背景的人愿意拨两下。

四条里**没有一条叫"够真实"**——这是刻意的。

---

## 10. 待你拧的刻度 (Dials — 我给了默认值,你可调)

这几处是真需要你划线的地方,我先填了建议默认值,不同意就改:

1. **术语预算**: 首屏+简报专业术语上限 = **5 个**(默认)。
2. **滑块上限**: 每领域**「我的动作」**旋钮 ≤ **3 个**(默认,维持现状)。牌面/环境/显示开关不计入(见 §6 修订)。
3. **诚实声明的可见度**: About 页明说"这是简化教学/娱乐模型,非真实预测" + 关键 proxy 打角标;**不在首屏喧宾夺主**(默认)。
4. **五领域上线顺序**: NEV 先行(进行中),其余四域顺序 = **待定**(建议先挑你最熟、最容易换皮的那个做第二个,验证迁移骨架)。

---

> **一句话记住这份宪章**: 基座保持干净可拆卸,微调(NEV)尽管做饱满;结构层较真,数值层放过;为 bug 和趣味回头,绝不为"更真实"回头。
